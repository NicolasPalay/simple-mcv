{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Listing of the categories</h1>
    <ul>
        {% for category in categories %}
            <li><a href="/category/show?id={{ category.id }}">{{ category.id }}. {{ category.name }}</a></li>
        {% else %}
            <li>Nothing to display</li>
        {% endfor %}
    </ul>
    <a href="/category/add">Ajouter</a>
{% endblock %}
